import React                   from 'react';
import _                       from 'lodash';
import { PropTypes }           from 'prop-types';
import assets                  from 'libs/assets';

export default class PaintProducts extends React.Component {
    static propTypes = {

    };

    constructor(props) {
        super(props);

        this.state = { };
    }

    render() {

        return (
            <div id="paint-product" className="container">

            </div>
        );
    }
}
